<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Active__c</fullName>
    <description>Indicates if a provider is active based on if the Inactive reason is populated.</description>
    <formula>IF(ISBLANK( TEXT(Inactive_Reason__c) ),TRUE,FALSE)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <inlineHelpText>This flag is determined by the Inactive Reason.  To make a provider &quot;Inactive&quot;, populate an Inactive reason.  To make them Active, remove the Inactive reason.</inlineHelpText>
    <label>Active</label>
    <trackHistory>false</trackHistory>
    <type>Checkbox</type>
</CustomField>
