<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Ready_to_Import_Validation</fullName>
    <active>true</active>
    <description>Validates the correct fields are populated before the status can be changed.</description>
    <errorConditionFormula>AND(TEXT(Status__c)=&quot;Ready for Import&quot;,ISBLANK(Primary_Location__c),ISBLANK(Practice_Group__c))</errorConditionFormula>
    <errorMessage>Practice &amp; Group, Primary Location need to be populated before this is ready for import.</errorMessage>
</ValidationRule>
